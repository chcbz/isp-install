# Pinned for the Python 3.6 runtime currently installed on the managed Agent host.
# The installer creates a release-local venv and validates every import before cutover.
python-docx==0.8.11
python-pptx==0.6.23
openpyxl==3.1.3
Pillow==8.4.0
PyPDF2==1.28.6
reportlab==3.6.8
