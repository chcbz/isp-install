---
name: openssl-install
description: Use only when the user explicitly asks for the repository's deprecated OpenSSL installer. In normal cases, the system OpenSSL should be used instead.
---

# OpenSSL Install

This installer is deprecated.

## Command

- Legacy use only: `sudo ./shell/openssl_install.sh`

## Guidance

- Prefer the system-provided OpenSSL on supported Linux distributions.
