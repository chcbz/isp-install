---
name: fonts-install
description: Use only when the user explicitly asks for the repository's deprecated fonts installer. The script is marked unsafe because it depends on hard-coded external sources.
---

# Fonts Install

This installer is deprecated and not recommended.

## Command

- Legacy use only: `sudo ./shell/fonts_install.sh`

## Guidance

- Prefer OS package manager font packages on new hosts.
