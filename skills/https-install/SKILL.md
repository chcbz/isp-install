---
name: https-install
description: Use only when the user explicitly asks for the repository's deprecated HTTPS helper. The script is not recommended for current deployments because it relies on hard-coded domains.
---

# HTTPS Install

This installer is deprecated and not recommended.

## Command

- Legacy use only: `sudo ./shell/https_install.sh`

## Guidance

- Prefer explicit Nginx or certificate management steps on modern hosts.
